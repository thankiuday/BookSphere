# Production Environment Variables for Render
# IMPORTANT: Set these in your Render dashboard under Environment Variables
# Do NOT commit actual values to version control

# Database - REQUIRED
# Get this from MongoDB Atlas (https://cloud.mongodb.com)
# Format: mongodb+srv://username:password@cluster.mongodb.net/booksphere?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/booksphere?retryWrites=true&w=majority

# Frontend URL - REQUIRED
# This should be your Render frontend URL
FRONTEND_URL=https://your-app-name.onrender.com

# JWT Secret - REQUIRED
# Generate a strong random string (at least 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-key-here

# OpenAI API Key - REQUIRED
# Get this from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# AWS S3 Configuration - REQUIRED
# Get these from AWS IAM console
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-s3-bucket-name

# Server Configuration - REQUIRED
PORT=10000
NODE_ENV=production
